/* eslint-env mocha */

require('mocha')
var assert = require('chai').assert

describe('tests are in the process of being written!', function () {
  it('placeholder test case', function () {
    var a = 1
    var b = 1
    assert.strictEqual(a, b)
  })
})
